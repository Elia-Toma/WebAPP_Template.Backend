{
    "Logging": {
        "LogLevel": {
            "Default": "Information",
            "Microsoft.AspNetCore": "Warning"
        }
    },
    "ConnectionStrings": { // Connection string for local SQL Database
        "MyContext": "Server=localhost;Database=db-name;User ID=user-id;Password=pwd;Trusted_Connection=False;Encrypt=True;TrustServerCertificate=True"
        // Substitute db-name, user-id, and pwd with your own values
    },
    "Cors": {
        "Origins": [ "https://localhost:7019", "http://localhost:5173" ]
    },
    "AllowedHosts": "*"
}
